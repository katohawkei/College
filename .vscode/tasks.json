{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build & run C++ project",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "command": "cd ./build && make ./${fileBasenameNoExtension}"
        }
    ]
}